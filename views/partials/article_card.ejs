
    <div class="col">
        <div class="card">
            <div class="top d-flex flex-row align-items-center"> <img src="<%= article.photo  %> " width="40" class="rounded-circle mr-2">
                <h6 class="mb-0 mr-2">Organics by LaMana</h6> <i class="fa fa-opencart"></i>
            </div>
            <div class="middle"> <img src="https://i.imgur.com/b4nm7pc.png" width="300" class="mt-3">
                <h5><%= article.titre %> </h5> 
                <span>
                    <%= article.description  %>
                </span>
            </div>
            <div class="bottom d-flex flex-row justify-content-center">
                <div class="input-group mb-3"> 
                    <span class="input-group-text">+</span> 
                    <input type="text" class="form-control" value="1"> 
                    <span class="input-group-text">-</span> 
                </div> 
                <button class="btn btn-success btn-sm add">
                    Add For <% if(article.prix){ %>
                        <%= article.prix.toFixed(2) %>
                            <% }else{ %>
                                0000.00
                                <% } %>  
                </button>
            </div>
        </div>
    </div>